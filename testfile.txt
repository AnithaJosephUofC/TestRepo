This is a test repository

Commands i have used toi onstall the git in my system in Terminal
-------------------------------------------------------
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install git
git --version


Then I have created a folder in my system desktop to clone the remote repo
I have cloned this git repo to my local system using the below command
git clone "url path for git repo"

go to the repo path and then start working! 

Now i have created a file in remote repo and now i want the file in my local. for that use the below command

